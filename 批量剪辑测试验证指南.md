# CutClaw 批量剪辑 — 测试验证指南

> 适用分支: `feat/batch-editing`
> 目标素材: `/Volumes/Data/Media/20251001青甘小环线/视频` (42 个 DJI/Nikon 4K HEVC 视频)
> 日期: 2026-05-28

---

## 1. 环境准备

### 1.1 激活已有 Conda 环境

```bash
conda activate CutClaw
cd ~/Develop/CutClaw
```

验证核心依赖:

```bash
python -c "
import torch; print(f'torch {torch.__version__}, cuda={torch.cuda.is_available()}')
import cv2; print(f'opencv {cv2.__version__}')
import decord; print('decord OK')
import litellm; print('litellm OK')
"
```

预期输出 (macOS Apple Silicon):
```
torch 2.8.0, cuda=False
opencv 4.13.0
decord OK
litellm OK
```

### 1.2 验证 ffmpeg 可用

```bash
ffmpeg -version | head -3
```

预期: `ffmpeg version 8.0.1` (或更高), 编译配置包含 `--enable-libx264 --enable-libx265`。

### 1.3 如需从零重建环境

```bash
conda create -n CutClaw python=3.12 -y
conda activate CutClaw
pip install -r requirements.txt
```

注意: `madmom` 可能需要 `pip install madmom --no-build-isolation`。
`pyannote.audio` 需要 HuggingFace token (仅 film 模式 ASR diarization 用到, vlog 模式不需要)。

### 1.4 验证新模块可导入

```bash
python -c "
from src.project.project import ProjectManager, Project, Clip
from src.project.media_profiles import BILIBILI_4K, get_profile, ffmpeg_output_args
from src.batch.checkpoint import CheckpointManager
from src.batch.quality_filter import QualityFilter
from src.batch.source_media_review import SourceMediaReview
from src.batch.material_index import MaterialIndex, build_material_index
from src.batch.batch_preprocess import BatchPreprocessor
print('All batch modules imported OK')
"
```

---

## 2. 测试流程 — 分步执行

### Step 1: 创建项目

**目的**: 扫描视频目录, ffprobe 提取元数据, 按天分组, 生成 `project.json`。

```bash
python local_run.py --project create \
  --video-dir "/Volumes/Data/Media/20251001青甘小环线/视频" \
  --project-name "青甘小环线自驾游"
```

**预期输出**:
```
📂 Creating project from: /Volumes/Data/Media/20251001青甘小环线/视频
  📎 Probing: DJI_20251002182137_0005_D.MP4 ... 16.2s, 3840x2160, hevc
  📎 Probing: DJI_20251003114415_0007_D.MP4 ... 45.8s, 3840x2160, hevc
  ... (42 lines total)

✅ Project created: Output/Projects/20251001_青甘小环线/project.json
Project: 青甘小环线自驾游 (20251001_青甘小环线)
Source:  /Volumes/Data/Media/20251001青甘小环线/视频
Output:  Output/Projects/20251001_青甘小环线
Clips:   42 files, XX.X min, XX.X GB
Days:    5
Progress: 0 done, 0 failed, 42 pending
```

**验证点**:
- [ ] 42 个文件全部成功 probe (无 skipped)
- [ ] 每个文件显示正确的分辨率 (3840x2160) 和编码 (hevc)
- [ ] Days 数量合理 (应为 5 天: 10/02, 10/03, 10/04, 10/05, 10/06)
- [ ] `Output/Projects/20251001_青甘小环线/project.json` 文件存在且内容完整

**查看生成的项目**:
```bash
python local_run.py --project status \
  --project-path "Output/Projects/20251001_青甘小环线/project.json"
```

**检视 project.json**:
```bash
python -c "
import json
with open('Output/Projects/20251001_青甘小环线/project.json') as f:
    p = json.load(f)
print(f'clips: {len(p[\"clips\"])}')
print(f'days: {len(p[\"days\"])}')
for d in p['days']:
    print(f'  Day {d[\"day_idx\"]} ({d[\"date\"]}): {len(d[\"clip_ids\"])} clips')
"
```

---

### Step 2: 源素材审查

**目的**: 检查所有素材的编码/分辨率/帧率/色彩空间一致性, 识别需要归一化的文件。

```bash
python local_run.py --project review-sources \
  --project-path "Output/Projects/20251001_青甘小环线/project.json"
```

**预期输出**:
```
Source Media Review: 20251001_青甘小环线
  Total clips: 42
  Resolutions: 3840x2160
  Codecs:      hevc
  FPS:         59.9
  Color space: bt2020_hdr (或类似)
  Needs normalization: No (或 YES, 如果有 Nikon 和 DJI 混合)
  Issues (N):
    [info] DJI_xxx: Very short clip: 2.1s
    ...
```

**验证点**:
- [ ] 分辨率是否统一 (预期: 全部 3840x2160)
- [ ] 编码是否统一 (预期: 全部 hevc)
- [ ] 帧率是否统一 (预期: 59.9 或 59.94)
- [ ] 色彩空间标记是否正确 (DJI 通常是 BT.2020 HLG)
- [ ] `Output/Projects/20251001_青甘小环线/source_review.json` 已生成

**如果 Needs normalization = YES**:
说明素材间有编码/分辨率/帧率差异, 后续渲染时 MultiSourceRenderer 会自动归一化。
这不影响预处理, 只影响最终渲染策略。

---

### Step 3: 批量预处理 (核心步骤, 耗时最长)

**目的**: 对每个 clip 执行完整的预处理链路 (帧提取 → 镜头检测 → 视频描述 → 场景合并 → 场景分析)。

#### 3a. 先用单 clip 验证链路

```bash
# 先只跑 1 个 clip, 确认链路通
python -c "
from src.project.project import ProjectManager
p = ProjectManager.load('Output/Projects/20251001_青甘小环线/project.json')
first_clip = p.clips[0]
print(f'Testing with: {first_clip.clip_id} ({first_clip.duration_sec:.1f}s)')
print(f'File: {first_clip.file_path}')
"
```

记录第一个 clip_id, 然后:

```bash
# 单 clip 测试 (需修改 batch_preprocess.py 支持 --clip-ids, 或直接在 Python 中调用)
conda run -n CutClaw python -c "
from src.project.project import ProjectManager
from src.batch.batch_preprocess import BatchPreprocessor

p = ProjectManager.load('Output/Projects/20251001_青甘小环线/project.json')
bp = BatchPreprocessor(p, max_workers=1, video_type='vlog')
bp.run(clip_ids=[p.clips[0].clip_id])
"
```

**预期**:
- 每个步骤有 emoji 标记的进度输出
- 最终显示 `✅ [clip_id] Done in XX.Xs (quality=XX)`
- `Output/Projects/20251001_青甘小环线/Clips/<clip_id>/` 下生成:
  - `frames/` — 抽帧结果
  - `captions/` — caption JSON
  - `captions/scenes/` — 场景分割 JSON
  - `captions/scene_summaries_video/` — 场景描述 JSON
  - `checkpoints/preprocess_<clip_id>.json` — checkpoint (status=completed)

**验证点**:
- [ ] 无报错退出
- [ ] `scene_summaries_video/` 下有 `scene_*.json` 文件 (数量取决于视频长度和场景数)
- [ ] checkpoint 标记为 completed
- [ ] quality_score 合理 (航拍素材通常 70-100, 不应低于 30)

#### 3b. 全量预处理

单 clip 验证通过后, 全量跑:

```bash
python local_run.py --project preprocess \
  --project-path "Output/Projects/20251001_青甘小环线/project.json" \
  --type vlog \
  --max-workers 2
```

**参数说明**:
| 参数 | 值 | 说明 |
|------|-----|------|
| `--type` | `vlog` | 无人机/旅行素材无对白, 跳过 ASR, 更快 |
| `--max-workers` | `2` | 并行数。M 系列 Mac 建议 1-2, GPU 机器可到 4 |

**预期输出** (42 个 clip):
```
🚀 Preprocessing 42 clips with 2 workers...
  🎞️  [DJI_20251002182137_0005_D] Decoding & shot detection...
  🎬 [DJI_20251002182137_0005_D] Video captioning...
  🧩 [DJI_20251002182137_0005_D] Scene merge...
  🔍 [DJI_20251002182137_0005_D] Scene analysis...
  ✅ [DJI_20251002182137_0005_D] Done in 120.3s (quality=85)
  ... (repeated for each clip)

============================================================
📊 Preprocessing complete:
   Total: 42, Success: 42, Skipped: 0, Failed: 0
============================================================
```

**中断续跑测试**:
1. 跑到一半按 `Ctrl+C`
2. 重新执行相同命令
3. 预期: 已完成的 clip 显示 `⏭️ already done, skipping`, 只处理剩余的

**验证点**:
- [ ] 42 个 clip 全部成功 (Success=42, Failed=0)
- [ ] 每个 clip 的 `scene_summaries_video/` 目录非空
- [ ] `checkpoints/` 目录下有 42 个 `preprocess_*.json`
- [ ] project.json 中 `preprocess_progress` 全部为 "done"
- [ ] 中断后续跑能正确跳过已完成的 clip

**检视预处理结果**:
```bash
# 检查每个 clip 的场景数
python -c "
import os
base = 'Output/Projects/20251001_青甘小环线/Clips'
for clip_dir in sorted(os.listdir(base))[:5]:
    scene_dir = os.path.join(base, clip_dir, 'captions', 'scene_summaries_video')
    if os.path.isdir(scene_dir):
        count = len([f for f in os.listdir(scene_dir) if f.endswith('.json')])
        print(f'{clip_dir}: {count} scenes')
"
```

---

### Step 4: 构建素材索引

**目的**: 汇总所有 clip 的场景描述, 生成全局扁平索引 `material_index.json`, 供后续锦书策划使用。

```bash
python local_run.py --project build-index \
  --project-path "Output/Projects/20251001_青甘小环线/project.json"
```

**预期输出**:
```
📊 Building material index for 20251001_青甘小环线...
✅ Material index built: XXX scenes from 42 clips
💾 Saved to: Output/Projects/20251001_青甘小环线/material_index.json
```

**验证点**:
- [ ] scene 总数合理 (42 个 clip, 每个 3-10 个场景, 预期 100-400 个 scene)
- [ ] `material_index.json` 文件存在且可解析
- [ ] 每个 SceneEntry 包含 `clip_file_path` (绝对路径) 和正确的时间范围

**检视索引内容**:
```bash
python -c "
import json
with open('Output/Projects/20251001_青甘小环线/material_index.json') as f:
    idx = json.load(f)
print(f'Total scenes: {idx[\"total_scenes\"]}')
print(f'Total duration: {idx[\"total_duration_sec\"]/60:.1f} min')
print(f'Valid clips: {idx[\"valid_clips\"]}')
print()
for s in idx['scenes'][:5]:
    print(f'  {s[\"scene_id\"]} | {s[\"clip_id\"]} | {s[\"start_sec\"]:.1f}-{s[\"end_sec\"]:.1f}s | {s[\"caption\"][:60]}')
"
```

---

## 3. 验证清单总览

| Step | 命令 | 耗时 | 关键验证 |
|------|------|------|----------|
| 1. 创建项目 | `--project create` | ~30s | 42 文件全部 probe, 按天分 5 组 |
| 2. 源素材审查 | `--project review-sources` | ~10s | codec/resolution/fps 一致性 |
| 3. 批量预处理 | `--project preprocess` | 30-60min | 每 clip 有场景 JSON, checkpoint 续跑正常 |
| 4. 构建索引 | `--project build-index` | ~5s | material_index.json, 100-400 scenes |

---

## 4. 故障排查

### 4.1 ffprobe 报错 "No such file"

**原因**: 外置硬盘 `/Volumes/Data` 未挂载。
**解决**: 确认硬盘已连接并挂载, `ls /Volumes/Data/` 有内容。

### 4.2 预处理某 clip 失败

**查看错误详情**:
```bash
cat Output/Projects/20251001_青甘小环线/checkpoints/preprocess_<clip_id>.json
```
常见原因:
- `OutOfMemoryError`: 降低 `--max-workers` 为 1
- `decord` 加载失败: 检查视频文件是否损坏 (`ffprobe <file>`)
- VLM API 超时: 检查网络和 API key 配置 (`config_my.py`)

### 4.3 预处理后场景数为 0

**原因**: shot detection 阈值太高, 或视频内容过于单一。
**排查**:
```bash
# 检查 shot_scenes.txt 是否有内容
cat Output/Projects/20251001_青甘小环线/Clips/<clip_id>/frames/shot_scenes.txt
```
**解决**: 可通过 `--config.SHOT_DETECTION_THRESHOLD 1.0` 降低阈值。

### 4.4 quality_filter 误判废片

航拍素材有时被标记为 "too_dark" 或 "blurry" (夜间/运动模糊)。
这不影响预处理, 只影响 `is_valid` 标记。
可在后续 Phase 中调整 QualityFilter 阈值。

### 4.5 conda 环境中缺少模块

```bash
conda activate CutClaw
pip install <missing_module>
```

---

## 5. 目录结构预期 (完成 Step 1-4 后)

```
Output/Projects/20251001_青甘小环线/
├── project.json                           # Step 1 产出
├── source_review.json                     # Step 2 产出
├── material_index.json                    # Step 4 产出
├── checkpoints/
│   ├── preprocess_DJI_20251002182137_0005_D.json
│   ├── preprocess_DJI_20251003114415_0007_D.json
│   └── ... (42 files)
├── Clips/
│   ├── DJI_20251002182137_0005_D/
│   │   ├── frames/
│   │   │   └── shot_scenes.txt
│   │   └── captions/
│   │       ├── captions.json
│   │       ├── ckpt/
│   │       ├── scenes/
│   │       │   ├── scene_0.json
│   │       │   └── ...
│   │       └── scene_summaries_video/     # ← 锦书策划的数据源
│   │           ├── scene_0.json
│   │           └── ...
│   └── ... (42 clip directories)
```

---

## 6. Phase 3: 策划升级 — 生成 shot_plan

> 前置条件: Step 1-4 全部完成 (project.json + material_index.json 已就绪)

### Step 5: 验证新模块可导入

```bash
python -c "
from src.batch.scene_adapter import write_virtual_scene_dir, scene_entry_to_json
from src.planner_agent import PlannerAgent
print('Phase 3 modules imported OK')
"
```

### Step 6: 用 project 模式生成 shot_plan

```bash
python local_run.py --project plan \
  --project-path "Output/Projects/20251001_青甘小环线/project.json" \
  --profile bilibili_4k \
  --strategy "第一天用航拍大景开场，展现青海湖壮阔；第二天重点保留盐湖倒影镜头；整体节奏由快到慢，结尾用日落收尾"
```

**预期输出**:
```
📊 Loaded material index: XXX scenes from XX clips
🎬 Output profile: bilibili_4k (3840x2160)
🖊️  Generating shot plan (iteration 1)...
[PlannerAgent] [ok] Shot plan generated in XX.Xs
[PlannerAgent] 🔗 Injected source_clip/scene_id into XX shots

✅ Shot plan generated in XX.Xs
💾 Saved to: Output/Projects/20251001_青甘小环线/shot_plans/shot_plan_bilibili_4k.json

[clip] 分镜方案审查
🎯 主题：...
[music] 配乐段落：...
[film] 分镜列表：
   ...
📊 统计：XX 个镜头 | 总时长约 XXXs
```

**验证点**:
- [ ] 无报错退出
- [ ] shot_plan JSON 文件存在
- [ ] 每个 shot 包含 `source_clip` 和 `scene_id` 字段 (Phase 3 注入)
- [ ] `related_scene` 索引有效 (不超出 material_index 范围)
- [ ] 分镜主题与 strategy 相关

**检视 shot_plan**:
```bash
python -c "
import json
with open('Output/Projects/20251001_青甘小环线/shot_plans/shot_plan_bilibili_4k.json') as f:
    plan = json.load(f)
vs = plan.get('video_structure', [])
total_shots = sum(len(s.get('shot_plan',{}).get('shots',[])) for s in vs)
print(f'Sections: {len(vs)}, Total shots: {total_shots}')
for sec in vs:
    shots = sec.get('shot_plan',{}).get('shots',[])
    print(f'  {sec.get(\"overall_theme\",\"\")[:50]} | {len(shots)} shots')
    for s in shots[:3]:
        sc = s.get('source_clip', 'MISSING')
        sid = s.get('scene_id', 'MISSING')
        print(f'    #{s.get(\"id\",\"?\")} [{s.get(\"time_duration\",\"?\")}s] clip={sc} scene={sid}')
        if sc == 'MISSING' or sid == 'MISSING':
            print('    ⚠️ 缺少 source_clip 或 scene_id!')
"
```

### Step 7: 验证虚拟 scene 目录 (可选)

```bash
# 检查虚拟 scene 目录是否正确生成
python -c "
import json, os
vdir = 'Output/Projects/20251001_青甘小环线/virtual_scenes'
if os.path.isdir(vdir):
    files = sorted([f for f in os.listdir(vdir) if f.endswith('.json')])
    print(f'Virtual scenes: {len(files)} files')
    with open(os.path.join(vdir, files[0])) as f:
        data = json.load(f)
    print(f'First scene keys: {list(data.keys())}')
    print(f'time_range: {data.get(\"time_range\", {})}')
    va = data.get('video_analysis', {}).get('scene_caption', {}).get('scene_summary', {})
    print(f'narrative: {va.get(\"narrative\", \"EMPTY\")[:80]}')
    print(f'_clip_id: {data.get(\"_clip_id\", \"MISSING\")}')
else:
    print('虚拟目录不存在 (可能已被清理)')
"
```

**验证点**:
- [ ] 虚拟 scene JSON 格式正确 (有 `time_range`, `video_analysis.scene_caption.scene_summary`)
- [ ] `narrative` 字段非空 (Bug 1 修复后应有内容)
- [ ] `_clip_id` 元数据存在

---

## 7. Phase 4: 剪辑点升级 — 生成 shot_point v2.0

> 前置条件: Phase 3 完成 (shot_plan_bilibili_4k.json 已就绪)

### Step 8: 用 project 模式生成 shot_point

```bash
python local_run.py --project edit \
  --project-path "Output/Projects/20251001_青甘小环线/project.json" \
  --shot-plan "Output/Projects/20251001_青甘小环线/shot_plans/shot_plan_bilibili_4k.json" \
  --profile bilibili_4k
```

**预期输出**:
```
📊 已加载素材索引: XXX 个场景, XX 个有效 clip
🎬 输出配置: bilibili_4k (3840x2160)
✂️  [ShortVideoEditor] 批量模式：X 个 clip 待处理

🎬 处理 clip: DJI_20251002182137_0005_D (X 个 shots)
🚀 [DirectShotSelector] Starting direct shot selection
✅ [DirectShotSelector] LLM returned in XX.Xs
✅ [DirectShotSelector] Validation passed
🔗 [DirectShotSelector] Injected clip metadata into X shots
  ✅ DJI_20251002182137_0005_D: X shots
...

✅ [ShortVideoEditor] 批量 shot_point 生成完成：XX shots
💾 保存到: Output/Projects/20251001_青甘小环线/shot_points/shot_point_bilibili_4k.json
```

**验证点**:
- [ ] 无报错退出 (特别关注 `FileNotFoundError` — Bug 3 修复点)
- [ ] 每个 clip 的 DirectShotSelector 都成功返回
- [ ] shot_point v2.0 文件存在

**检视 shot_point v2.0**:
```bash
python -c "
import json
with open('Output/Projects/20251001_青甘小环线/shot_points/shot_point_bilibili_4k.json') as f:
    sp = json.load(f)
print(f'Version: {sp.get(\"version\", \"MISSING\")}')
print(f'Total shots: {len(sp.get(\"shots\", []))}')
for s in sp.get('shots', [])[:5]:
    clip_path = s.get('clip_file_path', 'MISSING')
    clip_id = s.get('clip_id', 'MISSING')
    scene_id = s.get('scene_id', 'MISSING')
    speed = s.get('speed', 'MISSING')
    transition = s.get('transition', 'MISSING')
    print(f'  [{s.get(\"start_sec\",0):.1f}-{s.get(\"end_sec\",0):.1f}s] clip={clip_id} scene={scene_id} speed={speed} trans={transition}')
    if clip_path == 'MISSING':
        print('    ⚠️ 缺少 clip_file_path!')
    if scene_id == '':
        print('    ⚠️ scene_id 为空!')
"
```

**关键验证 — 跨 clip 选片**:
```bash
python -c "
import json
with open('Output/Projects/20251001_青甘小环线/shot_points/shot_point_bilibili_4k.json') as f:
    sp = json.load(f)
clips_used = set(s.get('clip_id','') for s in sp.get('shots',[]))
print(f'使用的不同 clip 数: {len(clips_used)}')
if len(clips_used) > 1:
    print('✅ 跨 clip 选片成功！')
else:
    print('⚠️ 只用到了 1 个 clip，可能未实现跨 clip 选片')
"
```

**验证点**:
- [ ] 每个 shot 有 `clip_file_path` (v2.0 格式)
- [ ] 每个 shot 有 `clip_id`
- [ ] `scene_id` 不为空 (Bug 6 修复点)
- [ ] `speed` 和 `transition` 有默认值
- [ ] 使用了多个不同 clip (跨 clip 选片)

---

## 8. Phase 5: 渲染引擎 — 多源渲染

> 前置条件: Phase 4 完成 (shot_point_bilibili_4k.json 已就绪)

### Step 9: 验证渲染模块可导入

```bash
python -c "
from render.multi_source_renderer import MultiSourceRenderer, RenderResult
from render.multi_profile_renderer import MultiProfileRenderer
print('Phase 5 modules imported OK')
"
```

### Step 10: 用 project 模式渲染

```bash
python local_run.py --project render \
  --project-path "Output/Projects/20251001_青甘小环线/project.json" \
  --shot-point "Output/Projects/20251001_青甘小环线/shot_points/shot_point_bilibili_4k.json" \
  --profile bilibili_4k
```

**预期输出**:
```
🎬 输出配置: bilibili_4k (3840x2160)

🎥 [MultiSourceRenderer] 开始渲染
   平台: bilibili_4k (3840x2160)
   片段数: XX

📋 Step 1/4: 验证源片段...
  🎨 检测到 HDR 素材，将进行色彩空间转换
  ⚠️ 源片段格式不一致，需要重编码

✂️  Step 2/4: 提取 XX 个片段...
  ✅ 成功提取 XX/XX 个片段 (X 个色彩空间转换)

🔗 Step 3/4: 拼接 XX 个片段...
  ✅ 拼接完成

🎵 Step 4/4: 叠加 BGM...
  ✅ BGM 混合完成

✅ 渲染完成: XX.Xs, XX.XMB, XXX.Xs
```

**验证点**:
- [ ] 无报错退出
- [ ] 输出文件存在: `Output/Projects/20251001_青甘小环线/output/bilibili_4k.mp4`
- [ ] 文件大小合理 (不应为 0 或极小)
- [ ] 色彩空间转换被触发 (如果有 HDR 素材)
- [ ] 无黑帧警告 (或仅少量)
- [ ] 无音量异常警告

**验证输出视频**:
```bash
# 用 ffprobe 检查输出
ffprobe -v quiet -show_entries stream=codec_name,width,height,r_frame_rate,color_space,color_transfer \
  -of json "Output/Projects/20251001_青甘小环线/output/bilibili_4k.mp4"
```

**预期**:
- `codec_name`: `hevc` (bilibili_4k profile 使用 libx265)
- `width`: 3840, `height`: 2160
- `r_frame_rate`: `60/1`
- `color_space`: `bt709` (已从 HDR 转换)

**手动播放验证**:
```bash
open "Output/Projects/20251001_青甘小环线/output/bilibili_4k.mp4"
```

检查:
- [ ] 视频能正常播放
- [ ] 画面颜色正常 (不过暗/不过亮 — 色彩空间转换质量)
- [ ] 多个不同片段正确拼接 (无跳帧/黑屏)
- [ ] BGM 音量正常 (无爆音/无静音)
- [ ] 时长合理 (与 shot_plan 中的总时长一致)

### Step 11: 测试其他 profile (可选)

```bash
# 抖音竖版
python local_run.py --project render \
  --project-path "Output/Projects/20251001_青甘小环线/project.json" \
  --shot-point "Output/Projects/20251001_青甘小环线/shot_points/shot_point_bilibili_4k.json" \
  --profile douyin
```

**验证点**:
- [ ] 输出为 1080x1920 (竖版)
- [ ] 画面裁切正确 (无变形)

---

## 9. 端到端验证清单总览

| Phase | Step | 命令 | 耗时 | 关键验证 |
|-------|------|------|------|----------|
| 1 | 1 | `--project create` | ~30s | 42 文件全部 probe, 按天分 5 组 |
| 2 | 2 | `--project review-sources` | ~10s | codec/resolution/fps 一致性 |
| 2 | 3 | `--project preprocess` | 30-60min | 每 clip 有场景 JSON, checkpoint 续跑 |
| 2 | 4 | `--project build-index` | ~5s | material_index.json, 100-400 scenes |
| 3 | 5 | 模块导入验证 | 1s | scene_adapter, planner_agent 可导入 |
| 3 | 6 | `--project plan` | 1-3min | shot_plan 有 source_clip/scene_id |
| 3 | 7 | 虚拟目录验证 | 1s | scene JSON 格式正确 |
| 4 | 8 | `--project edit` | 5-15min | shot_point v2.0, 跨 clip 选片 |
| 5 | 9 | 模块导入验证 | 1s | renderer 可导入 |
| 5 | 10 | `--project render` | 2-10min | 输出视频可播放, 色彩正常 |
| 5 | 11 | 其他 profile | 2-10min | 竖版裁切正确 |

---

## 10. Phase 3-5 故障排查

### 10.1 Phase 3: shot_plan 中 source_clip 全为空

**原因**: `generate_project_shot_plan()` 注入逻辑失败。
**排查**:
```bash
python -c "
import json
with open('Output/Projects/20251001_青甘小环线/shot_plans/shot_plan_bilibili_4k.json') as f:
    plan = json.load(f)
for sec in plan.get('video_structure',[]):
    for s in sec.get('shot_plan',{}).get('shots',[]):
        if 'source_clip' not in s:
            print(f'Missing source_clip: shot #{s.get(\"id\",\"?\")}, related_scene={s.get(\"related_scene\")}')
"
```

### 10.2 Phase 3: virtual_scenes 中 narrative 全为空

**原因**: Bug 1 修复前的 material_index.json 已缓存。
**解决**: 删除旧索引并重建:
```bash
rm Output/Projects/20251001_青甘小环线/material_index.json
python local_run.py --project build-index --project-path "Output/Projects/20251001_青甘小环线/project.json"
```

### 10.3 Phase 4: FileNotFoundError: ''

**原因**: Bug 3 未修复，`audio_caption_path` 为空。
**解决**: 确认 BGM 配置已设置，或确认代码已修复。

### 10.4 Phase 4: shot_point 中所有 shot 来自同一 clip

**原因**: shot_plan 中所有 shots 的 `clip_file_path` 相同。
**排查**:
```bash
python -c "
import json
with open('Output/Projects/20251001_青甘小环线/shot_plans/shot_plan_bilibili_4k.json') as f:
    plan = json.load(f)
paths = set()
for sec in plan.get('video_structure',[]):
    for s in sec.get('shot_plan',{}).get('shots',[]):
        p = s.get('clip_file_path','')
        if p: paths.add(p)
print(f'不同的 clip_file_path: {len(paths)}')
for p in sorted(paths)[:5]: print(f'  {p}')
"
```

### 10.5 Phase 5: ffmpeg 报错 "No such file or directory"

**原因**: shot_point 中的 `clip_file_path` 指向不存在的文件。
**排查**:
```bash
python -c "
import json, os
with open('Output/Projects/20251001_青甘小环线/shot_points/shot_point_bilibili_4k.json') as f:
    sp = json.load(f)
for s in sp.get('shots',[]):
    p = s.get('clip_file_path','')
    if p and not os.path.exists(p):
        print(f'Missing: {p}')
"
```

### 10.6 Phase 5: 输出视频颜色异常 (过暗/过亮)

**原因**: HDR → SDR tone mapping 参数不理想。
**排查**: 检查源素材色彩空间:
```bash
ffprobe -v quiet -show_entries stream=color_space,color_transfer,color_primaries \
  -of json "源视频路径.MP4"
```

**临时解决**: 跳过色彩空间转换:
```python
# 在 config_my.py 中设置
COLOR_SPACE_CONVERSION = "skip"
```

### 10.7 Phase 5: 音量检测始终无警告

**原因**: Bug 1 修复前的代码已缓存。
**解决**: 确认 `_check_audio_levels` 使用正则解析。

---

## 11. 完整目录结构 (Phase 1-5 全部完成后)

```
Output/Projects/20251001_青甘小环线/
├── project.json                           # Phase 1
├── source_review.json                     # Phase 2
├── material_index.json                    # Phase 2
├── virtual_scenes/                        # Phase 3 (可能已被清理)
│   ├── scene_0.json
│   └── ...
├── shot_plans/                            # Phase 3
│   └── shot_plan_bilibili_4k.json
├── shot_points/                           # Phase 4
│   └── shot_point_bilibili_4k.json
├── output/                                # Phase 5
│   ├── bilibili_4k.mp4
│   └── douyin.mp4 (可选)
├── checkpoints/                           # Phase 2
│   └── preprocess_*.json (42 files)
└── Clips/                                 # Phase 2
    └── <clip_id>/
        ├── frames/
        ├── captions/
        │   └── scene_summaries_video/
        └── quality.json
```
